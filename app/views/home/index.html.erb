<!-- app/views/home.html.erb -->
<div class="relative w-full h-screen overflow-hidden">
  <!-- Video cover -->
  <%= video_tag 'hero1.mp4', 
    autoplay: true, 
    loop: true, 
    muted: true, 
    playsinline: true, 
    class: "absolute top-0 left-0 w-full h-full object-cover" %>

  <!-- Contenu par-dessus la vidéo -->
  <div class="absolute inset-0 bg-black bg-opacity-50 flex items-center justify-center flex-col text-center">
    <!-- Nouveau titre -->
    <h1 class="text-white text-4xl sm:text-5xl md:text-6xl font-bold leading-tight px-4 md:px-0">
Shop the Latest Trends !   
 </h1>

    <!-- Slogan -->
    <p class="text-white text-lg sm:text-xl md:text-2xl mt-4 max-w-2xl px-4 md:px-0 mb-6">
      Discover What You Love, Shop What You Need.
    </p>

    <!-- Bouton -->
    <%= link_to 'Get started', products_path, class: 'mt-6 text-white rounded bg-red-800 px-6 py-3 text-lg font-semibold hover:bg-red-600 transition-all duration-300' %>
  </div>
</div>



















