
<div class="container mx-auto bg-white py-8 px-4">
<h1 class=" text-sm text-gray-500"> <%= link_to @category.name, category_path(@category)%> >  <%= link_to @subcategory.name, category_subcategory_path(@category, @subcategory)%></h1>

<!-- Liste des produits -->
  <% if @products.any? %>
      <%= render "products/affiche" ,product: @products %>
  <% else %>
    <p class="text-gray-600">Aucun produit disponible pour cette catégorie.</p>
  <% end %>


</div>


